name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 21

      - name: Install sbt
        run: |
          echo "deb https://repo.scala-sbt.org/scalasbt/debian all main" | sudo tee /etc/apt/sources.list.d/sbt.list
          sudo curl -sL "https://keyserver.ubuntu.com/pks/lookup?op=get&search=0x99E82A75642AC823" | sudo apt-key add -
          sudo apt-get update
          sudo apt-get install -y sbt

      - name: Cache sbt / Coursier / Ivy
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/coursier
            ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-${{ hashFiles('**/build.sbt', 'project/**') }}
          restore-keys: ${{ runner.os }}-sbt-

      # Advisory only: the existing feature branches carry pre-existing scalafmt
      # drift, so a blocking format check would block the whole stack on
      # formatting rather than on test failures. Keep it visible but
      # non-blocking; a one-time repo-wide `scalafmtAll` on dev will clear the
      # drift, after which this can be made blocking again.
      - name: Check formatting (advisory)
        continue-on-error: true
        run: sbt scalafmtCheckAll

      - name: Compile and test
        run: sbt test
